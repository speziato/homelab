apiVersion: hostpathprovisioner.kubevirt.io/v1beta1
kind: HostPathProvisioner
metadata:
  name: hostpath-provisioner
spec:
  imagePullPolicy: Always
  storagePools:
    - name: "xfs"
      path: "/var/mnt/disk1/csi"
  workload:
    nodeSelector:
      kubernetes.io/os: linux