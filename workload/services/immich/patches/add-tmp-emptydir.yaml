# Source: immich/templates/machine-learning.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: immich-machine-learning
  namespace: default
spec:
  template:
    spec: 
      containers: 
        - name: main
          volumeMounts:
          - mountPath: /tmp
            name: tmp
      volumes: 
        - name: tmp
          emptyDir: {}